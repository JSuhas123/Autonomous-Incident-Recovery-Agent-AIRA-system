apiVersion: aira.io/v1
kind: Runbook
runbookId: RB-CONTAINER-CHECK-RESOURCE-LIMITS
semver: "1.0.0"
name: Check Container Resource Limits
description: Inspect configured container resource limits and requests.
lifecycle: DRAFT
owner:
  ownerType: system
  name: AIRA Container Reliability
  team: platform-reliability
execution:
  mode: OBSERVE
risk:
  level: LOW
  blastRadius: none
  reversible: true
parameters:
  required: [targetId]
steps:
  - id: check-resource-limits
    order: 1
    type: containers
    action: check_resource_limits
    parameters:
      targetId: "${parameters.targetId}"
verification:
  required: false
rollback:
  supported: false
tags: [containers, resources, limits, diagnostic]