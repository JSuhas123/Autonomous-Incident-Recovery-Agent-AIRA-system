apiVersion: aira.io/v1
kind: Runbook

runbookId: RB-OBS-CHECK-LOGS-FLOW
semver: "1.0.0"

name: Logs Flow Investigation

description: >
  Determines whether log telemetry is flowing from producers through the
  configured collection and ingestion path to its destination.

lifecycle: ACTIVE

owner:
  ownerType: system
  ownerId: aira-core
  name: AIRA Observability Reliability
  team: observability-reliability

scope:
  environments:
    - production
    - staging
  services:
    - observability
    - logs

risk:
  level: LOW
  blastRadius: none
  reversible: true

parameters:
  - name: targetId
    type: string
    required: true
    description: Registered observability diagnostic target identifier.

steps:
  - id: step-01
    name: Check Logs Flow
    order: 1
    type: observability
    action: check_logs_flow
    params:
      targetId: "${targetId}"
    failurePolicy: STOP

verification:
  strategy: ALL
  timeoutSeconds: 30
  checks:
    - id: check-01
      type: diagnostic_completed
      description: Log-flow evidence was collected.
      timeoutSeconds: 30
      optional: false

rollbackConfig:
  strategy: NONE

auditConfig:
  redactSensitiveValues: true

tags:
  - observability
  - logs
  - telemetry
  - diagnostic
  - read-only