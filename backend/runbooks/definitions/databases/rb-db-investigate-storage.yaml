apiVersion: aira.io/v1
kind: Runbook
runbookId: RB-DB-INVESTIGATE-STORAGE
semver: 1.0.0
name: Database Storage Pressure Investigation
description: Investigates database storage utilization and pressure without deleting or modifying data.
lifecycle: ACTIVE
owner:
  ownerType: system
  ownerId: aira-core
  name: AIRA Database Reliability
  team: database-reliability
scope:
  environments:
    - production
    - staging
  services:
    - database
risk:
  level: LOW
  blastRadius: none
  reversible: true
parameters:
  - name: targetId
    type: string
    required: true
    description: Identifier of an explicitly registered external database diagnostic target.
steps:
  - id: step-01
    name: Inspect database storage
    order: 1
    type: database
    action: get_storage
    params:
      targetId: ${targetId}
    failurePolicy: STOP
  - id: step-02
    name: Evaluate storage pressure
    order: 2
    type: database
    action: check_storage_pressure
    params:
      targetId: ${targetId}
    failurePolicy: STOP
  - id: step-03
    name: Read database health
    order: 3
    type: database
    action: get_health
    params:
      targetId: ${targetId}
    failurePolicy: CONTINUE
verification:
  strategy: ALL
  timeoutSeconds: 30
  checks:
    - id: check-01
      type: diagnostic_completed
      description: Database Storage Pressure Investigation completed and returned diagnostic evidence.
      timeoutSeconds: 30
      optional: false
rollbackConfig:
  strategy: NONE
auditConfig:
  redactSensitiveValues: true
tags:
  - database
  - diagnostic
  - read-only
  - phase-13
