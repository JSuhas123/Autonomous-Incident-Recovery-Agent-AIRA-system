apiVersion: aira.io/v1
kind: Runbook
runbookId: RB-NET-INVESTIGATE-LATENCY
semver: 1.0.0
name: Network Latency Investigation
description: Investigates elevated network latency and supporting path evidence.
lifecycle: ACTIVE
owner:
  ownerType: system
  ownerId: aira-core
  name: AIRA Networking Reliability
  team: networking-reliability
scope:
  environments:
    - production
    - staging
  services:
    - networking
risk:
  level: LOW
  blastRadius: none
  reversible: true
parameters:
  - name: targetId
    type: string
    required: true
    description: Identifier of an explicitly registered external network diagnostic target.
steps:
  - id: step-01
    name: Measure network latency
    order: 1
    type: networking
    action: check_latency
    params:
      targetId: ${targetId}
    failurePolicy: STOP
  - id: step-02
    name: Check packet loss
    order: 2
    type: networking
    action: check_packet_loss
    params:
      targetId: ${targetId}
    failurePolicy: CONTINUE
  - id: step-03
    name: Inspect network route
    order: 3
    type: networking
    action: check_route
    params:
      targetId: ${targetId}
    failurePolicy: CONTINUE
verification:
  strategy: ALL
  timeoutSeconds: 30
  checks:
    - id: check-01
      type: diagnostic_completed
      description: Network Latency Investigation completed and returned diagnostic evidence.
      timeoutSeconds: 30
      optional: false
rollbackConfig:
  strategy: NONE
auditConfig:
  redactSensitiveValues: true
tags:
  - networking
  - diagnostic
  - read-only
  - phase-13
