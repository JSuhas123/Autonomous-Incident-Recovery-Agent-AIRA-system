apiVersion: aira.io/v1
kind: Runbook
runbookId: RB-NET-INVESTIGATE-ROUTE
semver: 1.0.0
name: Network Route Investigation
description: Inspects network routing evidence without modifying routing tables.
lifecycle: ACTIVE
owner:
  ownerType: system
  ownerId: aira-core
  name: AIRA Networking Reliability
  team: networking-reliability
scope:
  environments:
    - production
    - staging
  services:
    - networking
risk:
  level: LOW
  blastRadius: none
  reversible: true
parameters:
  - name: targetId
    type: string
    required: true
    description: Identifier of an explicitly registered external network diagnostic target.
steps:
  - id: step-01
    name: Inspect network route
    order: 1
    type: networking
    action: check_route
    params:
      targetId: ${targetId}
    failurePolicy: STOP
  - id: step-02
    name: Check end-to-end connectivity
    order: 2
    type: networking
    action: check_connectivity
    params:
      targetId: ${targetId}
    failurePolicy: CONTINUE
verification:
  strategy: ALL
  timeoutSeconds: 30
  checks:
    - id: check-01
      type: diagnostic_completed
      description: Network Route Investigation completed and returned diagnostic evidence.
      timeoutSeconds: 30
      optional: false
rollbackConfig:
  strategy: NONE
auditConfig:
  redactSensitiveValues: true
tags:
  - networking
  - diagnostic
  - read-only
  - phase-13
