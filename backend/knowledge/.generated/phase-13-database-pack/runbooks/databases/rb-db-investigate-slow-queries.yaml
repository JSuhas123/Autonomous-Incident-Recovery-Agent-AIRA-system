apiVersion: aira.io/v1
kind: Runbook
runbookId: RB-DB-INVESTIGATE-SLOW-QUERIES
semver: 1.0.0
name: Database Slow Query Investigation
description: Collects slow-query and transaction evidence without terminating queries or changing indexes.
lifecycle: ACTIVE
owner:
  ownerType: system
  ownerId: aira-core
  name: AIRA Database Reliability
  team: database-reliability
scope:
  environments:
    - production
    - staging
  services:
    - database
risk:
  level: LOW
  blastRadius: none
  reversible: true
parameters:
  - name: targetId
    type: string
    required: true
    description: Identifier of an explicitly registered external database diagnostic target.
steps:
  - id: step-01
    name: Collect slow queries
    order: 1
    type: database
    action: get_slow_queries
    params:
      targetId: ${targetId}
    failurePolicy: STOP
  - id: step-02
    name: Inspect long transactions
    order: 2
    type: database
    action: get_long_transactions
    params:
      targetId: ${targetId}
    failurePolicy: CONTINUE
  - id: step-03
    name: Inspect locks
    order: 3
    type: database
    action: get_locks
    params:
      targetId: ${targetId}
    failurePolicy: CONTINUE
verification:
  strategy: ALL
  timeoutSeconds: 30
  checks:
    - id: check-01
      type: diagnostic_completed
      description: Database Slow Query Investigation completed and returned diagnostic evidence.
      timeoutSeconds: 30
      optional: false
rollbackConfig:
  strategy: NONE
auditConfig:
  redactSensitiveValues: true
tags:
  - database
  - diagnostic
  - read-only
  - phase-13
